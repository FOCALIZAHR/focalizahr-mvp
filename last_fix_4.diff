diff --git a/src/app/dashboard/evaluaciones/[assignmentId]/summary/page.tsx b/src/app/dashboard/evaluaciones/[assignmentId]/summary/page.tsx
index 1ff7c23..6660d6c 100644
--- a/src/app/dashboard/evaluaciones/[assignmentId]/summary/page.tsx
+++ b/src/app/dashboard/evaluaciones/[assignmentId]/summary/page.tsx
@@ -11,6 +11,9 @@ import CinemaSummaryOrchestrator from '../components/CinemaSummaryOrchestrator'
 import TeamCalibrationHUD from '@/components/performance/TeamCalibrationHUD'
 import InsightCarousel from '@/components/performance/summary/InsightCarousel'
 import PerformanceScoreCard from '@/components/performance/PerformanceScoreCard'
+import PotentialNineBoxCard from '@/components/performance/PotentialNineBoxCard'
+import { GhostButton } from '@/components/ui/PremiumButton'
+import { Eye } from 'lucide-react'
 import type { CinemaSummaryData } from '@/types/evaluator-cinema'
 
 // ════════════════════════════════════════════════════════════════════════════
@@ -42,6 +45,13 @@ export default function EvaluationSummaryPage() {
     score: number
   }>>([])
 
+  // Estado para datos de potencial (9-Box)
+  const [potentialData, setPotentialData] = useState<{
+    potentialScore: number | null
+    potentialLevel: string | null
+    nineBoxPosition: string | null
+  } | null>(null)
+
   useEffect(() => {
     async function fetchSummary() {
       try {
@@ -118,8 +128,8 @@ export default function EvaluationSummaryPage() {
             .map((a: any) => ({
               id: a.evaluatee?.id || a.id,
               name: a.evaluatee?.fullName || 'Sin nombre',
-              // avgScore del API /assignments está en escala 0-100, convertir a 1-5
-              score: a.avgScore / 20
+              // avgScore del API /assignments ya viene en escala 1-5
+              score: a.avgScore
             }))
             .sort((a: any, b: any) => b.score - a.score)
 
@@ -134,6 +144,52 @@ export default function EvaluationSummaryPage() {
     fetchTeamData()
   }, [])
 
+  // ═══════════════════════════════════════════════════════════════════════════
+  // Fetch datos de potencial y 9-Box del empleado
+  // ═══════════════════════════════════════════════════════════════════════════
+  useEffect(() => {
+    async function fetchPotentialData() {
+      if (!summary?.cycle?.id || !summary?.evaluatee?.id) return
+
+      try {
+        const token = localStorage.getItem('focalizahr_token')
+        if (!token) return
+
+        const res = await fetch(
+          `/api/performance-ratings/nine-box?cycleId=${summary.cycle.id}`,
+          {
+            headers: {
+              'Authorization': `Bearer ${token}`,
+              'Content-Type': 'application/json'
+            }
+          }
+        )
+
+        if (!res.ok) return
+
+        const json = await res.json()
+        if (json.success && json.data.grid) {
+          // Buscar el empleado actual en toda la grid
+          const employeeData = json.data.grid
+            .flatMap((cell: any) => cell.employees)
+            .find((emp: any) => emp.employeeId === summary.evaluatee.id)
+
+          if (employeeData) {
+            setPotentialData({
+              potentialScore: employeeData.potentialScore,
+              potentialLevel: employeeData.potentialLevel,
+              nineBoxPosition: employeeData.nineBoxPosition
+            })
+          }
+        }
+      } catch (err) {
+        console.error('[Summary] Error fetching potential data:', err)
+      }
+    }
+
+    fetchPotentialData()
+  }, [summary])
+
   // ═══════════════════════════════════════════════════════════════════════════
   // Calcular competencias desde categorizedResponses para ManagementAlertsHUD
   // ═══════════════════════════════════════════════════════════════════════════
@@ -259,6 +315,28 @@ export default function EvaluationSummaryPage() {
             />
           )}
 
+          {/* PotentialNineBoxCard + Boton Ver Resumen */}
+          {potentialData && (
+            <div className="flex items-start gap-3">
+              <PotentialNineBoxCard
+                potentialScore={potentialData.potentialScore}
+                potentialLevel={potentialData.potentialLevel}
+                nineBoxPosition={potentialData.nineBoxPosition}
+                showTeslaLine={true}
+                className="flex-1"
+              />
+              <GhostButton
+                icon={Eye}
+                size="sm"
+                onClick={() => {
+                  console.log('Ver resumen completo')
+                }}
+              >
+                Ver Resumen
+              </GhostButton>
+            </div>
+          )}
+
           {/* TeamCalibrationHUD - ranking del equipo */}
           {teamMembers.length > 0 ? (
             <TeamCalibrationHUD
