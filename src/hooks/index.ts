// Hooks centralizados para FocalizaHR Dashboard
export { useMetrics } from './useMetrics';
export { useCampaigns } from './useCampaigns';

// Re-export types if needed
export type { 
  UseMetricsParams, 
  UseMetricsReturn 
} from './useMetrics';

export type { 
  UseCampaignsParams, 
  UseCampaignsReturn 
} from './useCampaigns';